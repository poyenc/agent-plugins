{
  "description": "Injects branch-aware knowledge (directives, user profile, topic indexes, task state) into session context at start",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/session-start.sh"
          }
        ]
      }
    ]
  }
}
